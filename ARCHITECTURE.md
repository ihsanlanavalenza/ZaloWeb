# 🗺️ Portfolio Architecture Diagram

## 📊 Struktur Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     CURRENT (Frontend Only)                  │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│  usePortfolio.js     │  ← Edit konten di sini (sekarang)
│  (Composable)        │
└──────────┬───────────┘
           │
           │ return { header, items, stats, cta }
           ↓
┌──────────────────────┐
│  StatsSection.vue    │  ← Component (sudah dynamic, jarang edit)
│  (Component)         │
└──────────┬───────────┘
           │
           │ Render UI
           ↓
┌──────────────────────┐
│  Browser             │  ← Hasil tampilan
│  (User sees)         │
└──────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                    FUTURE (With CMS/Backend)                 │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│  Database / CMS      │  ← Data storage (MySQL/Postgres/Strapi)
│  (MySQL, Strapi)     │
└──────────┬───────────┘
           │
           │ SQL Query / API
           ↓
┌──────────────────────┐
│  portfolio.get.ts    │  ← API endpoint (backend)
│  (Server API)        │
└──────────┬───────────┘
           │
           │ REST API / JSON
           ↓
┌──────────────────────┐
│  usePortfolio.js     │  ← Fetch dari API
│  (Composable)        │
└──────────┬───────────┘
           │
           │ return data
           ↓
┌──────────────────────┐
│  StatsSection.vue    │  ← Component (tetap sama)
│  (Component)         │
└──────────┬───────────┘
           │
           │ Render UI
           ↓
┌──────────────────────┐
│  Browser             │  ← Hasil tampilan
│  (User sees)         │
└──────────────────────┘
```

## 🏗️ File Structure Diagram

```
web/
│
├── 📄 DOCS_INDEX.md              ← START HERE: Dokumentasi utama
├── 📄 QUICK_START.md             ← Edit konten cepat
├── 📄 PORTFOLIO_SUMMARY.md       ← Overview struktur
├── 📄 PORTFOLIO_README.md        ← Dokumentasi teknis
│
├── components/
│   └── 📄 StatsSection.vue       ← UI Component (Dynamic)
│                                    ↑
│                                    │ uses
│                                    │
├── composables/
│   └── 📄 usePortfolio.js        ← DATA SOURCE (Edit di sini!)
│                                    │
│                                    │ (nanti) fetch from ↓
│
├── server/
│   └── api/
│       └── 📄 portfolio.get.ts   ← API Endpoint (untuk backend)
│                                    │
│                                    │ (nanti) query from ↓
│
│                                 [Database]
│                                 MySQL / Postgres
│                                 atau CMS (Strapi)
│
├── config/
│   └── 📄 portfolio.config.ts    ← Style Configuration
│
├── types/
│   └── 📄 portfolio.ts           ← TypeScript Types
│
├── utils/
│   └── 📄 portfolio.utils.ts     ← Helper Functions
│
├── data/
│   └── 📄 portfolio.example.json ← Contoh structure data
│
└── public/
    └── images/
        └── project/
            ├── 1.png             ← Portfolio images
            ├── 2.png
            ├── ...
            └── 8.png
```

## 🎯 Component Interaction Diagram

```
┌────────────────────────────────────────────────────────┐
│                  StatsSection.vue                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │ <script setup>                                    │  │
│  │   const { header, items, stats, cta }             │  │
│  │     = usePortfolio()                              │  │
│  └────────────────┬──────────────────────────────────┘  │
│                   │                                      │
│  ┌────────────────▼──────────────────────────────────┐  │
│  │ <template>                                        │  │
│  │                                                   │  │
│  │  ┌──────────────────────────────────────┐        │  │
│  │  │ Header Section                        │        │  │
│  │  │ {{ header.badge }}                    │        │  │
│  │  │ {{ header.title }}                    │        │  │
│  │  │ {{ header.description }}              │        │  │
│  │  └──────────────────────────────────────┘        │  │
│  │                                                   │  │
│  │  ┌──────────────────────────────────────┐        │  │
│  │  │ Portfolio Grid                        │        │  │
│  │  │ v-for="item in items"                 │        │  │
│  │  │   ┌─────────────────────────┐         │        │  │
│  │  │   │ Card Type: hero         │         │        │  │
│  │  │   │ Card Type: feature      │         │        │  │
│  │  │   │ Card Type: project      │         │        │  │
│  │  │   │ Card Type: pricing      │         │        │  │
│  │  │   │ Card Type: about        │         │        │  │
│  │  │   │ Card Type: info         │         │        │  │
│  │  │   └─────────────────────────┘         │        │  │
│  │  └──────────────────────────────────────┘        │  │
│  │                                                   │  │
│  │  ┌──────────────────────────────────────┐        │  │
│  │  │ Stats Section                         │        │  │
│  │  │ v-for="stat in stats"                 │        │  │
│  │  │   {{ stat.value }} {{ stat.label }}   │        │  │
│  │  └──────────────────────────────────────┘        │  │
│  │                                                   │  │
│  │  ┌──────────────────────────────────────┐        │  │
│  │  │ CTA Button                            │        │  │
│  │  │ {{ cta.text }}                        │        │  │
│  │  │ href="{{ cta.link }}"                 │        │  │
│  │  └──────────────────────────────────────┘        │  │
│  │                                                   │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

## 🎨 Card Types Visual Reference

```
┌─────────────────────────────────────────────────────────┐
│  BENTO GRID LAYOUT (4 columns on desktop)              │
└─────────────────────────────────────────────────────────┘

Desktop View (lg screens):
┌───────────────┬───────────────┬───────┬───────┐
│               │               │       │       │
│               │   Feature     │  P1   │  P2   │
│     Hero      │   (2x1)       ├───────┼───────┤
│     (2x2)     ├───────────────┤       │       │
│               │   Pricing     │  P3   │  P4   │
│               │   (2x1)       │       │       │
├───────────────┴───────────────┴───────┴───────┤
│              About (2x1)              │ Info  │
│                                       │ (2x1) │
└───────────────────────────────────────┴───────┘

Tablet View (md screens):
┌───────────────┬───────────────┐
│               │               │
│     Hero      │   Feature     │
│     (2x2)     │   (2x1)       │
│               ├───────────────┤
│               │   Pricing     │
├───────────────┼───────────────┤
│     About     │      P1       │
│     (2x1)     ├───────┬───────┤
├───────────────┤  P2   │  P3   │
│     Info      ├───────┴───────┤
│     (2x1)     │      P4       │
└───────────────┴───────────────┘

Mobile View:
┌───────────────┐
│               │
│     Hero      │
│               │
├───────────────┤
│   Feature     │
├───────────────┤
│   Pricing     │
├───────────────┤
│     About     │
├───────────────┤
│      P1       │
├───────────────┤
│      P2       │
├───────────────┤
│      P3       │
├───────────────┤
│      P4       │
├───────────────┤
│     Info      │
└───────────────┘

Legend:
- Hero: Large featured project (2x2)
- Feature: Service/feature highlight (2x1)
- Pricing: Package pricing (2x1)
- About: Company/team info (2x1)
- P1-P4: Project cards (1x1)
- Info: Additional information (2x1)
```

## 🔄 Migration Path Diagram

```
┌──────────────────────────────────────────────────────────┐
│  PHASE 1: Frontend Only (SEKARANG) ✅                     │
└──────────────────────────────────────────────────────────┘

Data: usePortfolio.js (hardcoded)
           ↓
Component: StatsSection.vue
           ↓
Output: Static website


┌──────────────────────────────────────────────────────────┐
│  PHASE 2: With API (TRANSISI)                            │
└──────────────────────────────────────────────────────────┘

Data: usePortfolio.js → portfolio.get.ts → Database
           ↓
Component: StatsSection.vue (unchanged)
           ↓
Output: Dynamic website


┌──────────────────────────────────────────────────────────┐
│  PHASE 3: Full CMS (TARGET)                              │
└──────────────────────────────────────────────────────────┘

Admin Dashboard → Database → API → Frontend
                                ↓
                      StatsSection.vue
                                ↓
                      Dynamic website with
                      admin management
```

## 📱 Responsive Breakpoints

```
Mobile        Tablet       Desktop       Large
0px           768px        1024px        1280px
│             │            │             │
▼             ▼            ▼             ▼
1 col         2 cols       4 cols        4 cols
Stack         Grid 2x      Full Grid     Full Grid
Vertical      Mixed        Bento         Bento

<────────────>
  sm: 640px

<────────────────────────>
        md: 768px

<──────────────────────────────────>
              lg: 1024px

<────────────────────────────────────────────>
                    xl: 1280px
```

## 🎯 Data Structure Hierarchy

```
portfolioData
│
├── header
│   ├── badge
│   ├── title
│   └── description
│
├── items[]
│   ├── [0] Hero Card
│   │   ├── id
│   │   ├── type: "hero"
│   │   ├── image
│   │   ├── title
│   │   └── ...
│   │
│   ├── [1] Feature Card
│   │   ├── id
│   │   ├── type: "feature"
│   │   └── ...
│   │
│   └── [2-9] Other Cards
│
├── stats[]
│   ├── [0] { value, label }
│   ├── [1] { value, label }
│   ├── [2] { value, label }
│   └── [3] { value, label }
│
└── cta
    ├── text
    └── link
```

---

**Legend:**
- 📄 File
- 📁 Folder
- → Data flow
- ← Uses/Imports
- ↓ Next step
- ↑ Previous step

